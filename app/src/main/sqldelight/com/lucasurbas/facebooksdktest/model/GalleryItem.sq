CREATE TABLE galleryItem (
    _id TEXT NOT NULL PRIMARY KEY,
    path TEXT NOT NULL,
    post_id TEXT
);

select_all:
SELECT *
FROM galleryItem;

select_by_id:
SELECT *
FROM galleryItem
where galleryItem._id = ?;